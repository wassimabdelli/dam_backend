# ============================================
# DAM Backend - Environment Variables Template
# ============================================
# Copy this file to .env and fill in your actual values
# ⚠️ NEVER commit the .env file to version control!

# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
PORT=3001

# ============================================
# Database Configuration
# ============================================
# MongoDB connection string
# Format: mongodb://[username:password@]host[:port][/database][?options]
MONGO_URI=mongodb://127.0.0.1:27017/dam_database

# Alternative MongoDB URI format (for compatibility)
# MONGODB_URI=mongodb://localhost:27017/dam_backend

# ============================================
# JWT Configuration
# ============================================
# ⚠️ REQUIRED: Must be at least 20 characters long
# Generate a secure random string for production!
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_minimum_20_characters_long_change_this_in_production

# JWT token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: '3600s', '1h', '7d'
JWT_EXPIRATION_TIME=3600s

# ============================================
# Application URLs
# ============================================
# Backend URL (used for OAuth callbacks and email verification links)
# ⚠️ IMPORTANT: No trailing slash!
# Development: http://localhost:3001
# Production: https://your-domain.com
BACKEND_URL=http://localhost:3001

# Frontend URL (used for OAuth redirects after authentication)
# Development: http://localhost:4200
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:4200

# ============================================
# Google OAuth2 Configuration
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# See documentation for detailed setup instructions
#
# Steps to get credentials:
# 1. Go to Google Cloud Console
# 2. Create a project or select existing one
# 3. Enable Google+ API or Google Identity API
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add authorized redirect URI: {BACKEND_URL}/api/v1/auth/google/redirect
#
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google OAuth callback URL
# Usually auto-constructed from BACKEND_URL, but can be overridden
# Format: {BACKEND_URL}/api/v1/auth/google/redirect
GOOGLE_CALLBACK_URL=http://localhost:3001/api/v1/auth/google/redirect

# ============================================
# Vonage OTP Configuration (SMS)
# ============================================
# Get these from: https://dashboard.nexmo.com/
# Vonage (formerly Nexmo) is used for sending OTP codes via SMS
#
# Steps to get credentials:
# 1. Sign up at https://dashboard.nexmo.com/
# 2. Get your API Key and API Secret from the dashboard
# 3. Optionally configure a brand name for SMS sender
#
VONAGE_API_KEY=your_vonage_api_key_here
VONAGE_API_SECRET=your_vonage_api_secret_here

# Optional: Brand name for SMS sender (defaults to "Vonage" if not set)
VONAGE_BRAND_NAME=YourAppName

# ============================================
# Facebook OAuth2 Configuration (Optional)
# ============================================
# Get these from: https://developers.facebook.com/apps
# Only needed if you want to enable Facebook login
#
# Steps to get credentials:
# 1. Go to Facebook Developers
# 2. Create a new app
# 3. Add Facebook Login product
# 4. Configure OAuth redirect URI: {BACKEND_URL}/api/v1/auth/facebook/redirect
#
# FACEBOOK_APP_ID=your_facebook_app_id_here
# FACEBOOK_APP_SECRET=your_facebook_app_secret_here
# FACEBOOK_CALLBACK_URL=http://localhost:3001/api/v1/auth/facebook/redirect

# ============================================
# Email Configuration (Optional)
# ============================================
# Brevo (formerly Sendinblue) API for sending emails
# Only needed if email verification or notifications are enabled
#
# Steps to get credentials:
# 1. Sign up at https://www.brevo.com/
# 2. Get your API key from the dashboard
# 3. Configure sender email and name
#
# BREVO_API_KEY=your_brevo_api_key_here
# MAIL_FROM_EMAIL=noreply@yourdomain.com
# MAIL_FROM_NAME=Your App Name

# ============================================
# Cookie Configuration (Optional)
# ============================================
# Domain for cookies (only needed in production with multiple subdomains)
# COOKIE_DOMAIN=.yourdomain.com

# ============================================
# Notes
# ============================================
# 1. Replace all placeholder values with your actual credentials
# 2. Never commit this file with real credentials to version control
# 3. Use different values for development and production
# 4. For production, use strong, randomly generated secrets
# 5. Ensure BACKEND_URL matches your actual backend URL exactly
# 6. Google OAuth callback URL must match exactly in Google Cloud Console

